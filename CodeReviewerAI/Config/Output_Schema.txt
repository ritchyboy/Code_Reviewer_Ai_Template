### SYSTEM OUTPUT CONFIGURATION

**Mandatory Output Format:**
You must respond with a valid, raw JSON object. 
Do not wrap the output in markdown code blocks (like ```json ... ```).
Do not include any conversational text before or after the JSON.

**JSON Schema:**
{
  "is_approved": boolean,      // true ONLY if the code is safe, performant, and follows all architectural rules.
  "risk_score": integer,       // 0 (Safe) to 100 (Critical/Broken).
  "risk_level": string,        // "Low", "Medium", "High", or "Critical".
  "summary": string,           // A short, single-sentence summary of the verdict.
  "markdown_review": string    // The complete, formatted code review using Markdown (headers, lists, code blocks).
}

**Logic Rules for Fields:**
1. If "risk_score" is above 70, "is_approved" must be false.
2. "markdown_review" is what will be posted to GitHub. Make it professional and direct.